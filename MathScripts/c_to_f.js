"use strict";

//the known value
var celsius = 30;

//what we need to know
var fahrenheit = (celsius * 9/5) + 32;

//the result
var message = "The temperature in fahrenheit is " + Math.round(fahrenheit);

console.log(message);